# UpdatesList - Component Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────┐
│                    UpdatesList Component                     │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Blog       │  │ Publications │  │    Talks     │     │
│  │   Posts      │  │              │  │              │     │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘     │
│         │                 │                  │              │
│         └─────────────────┼──────────────────┘              │
│                           ↓                                 │
│                  ┌────────────────┐                         │
│                  │  Unified       │                         │
│                  │  UpdateItem[]  │                         │
│                  └────────┬───────┘                         │
│                           ↓                                 │
│           ┌───────────────────────────────┐                │
│           │  Sort by Date (newest first)  │                │
│           └───────────────┬───────────────┘                │
│                           ↓                                 │
│           ┌───────────────────────────────┐                │
│           │  Apply Filters & Grouping     │                │
│           └───────────────┬───────────────┘                │
│                           ↓                                 │
│           ┌───────────────────────────────┐                │
│           │  Render Timeline              │                │
│           └───────────────────────────────┘                │
└─────────────────────────────────────────────────────────────┘
```

## Component Structure

```
UpdatesList.astro
│
├─── Props Interface
│    ├── items: UpdateItem[]
│    ├── limit?: number
│    ├── compact?: boolean
│    ├── showType?: boolean
│    ├── groupByMonth?: boolean
│    ├── filterTypes?: string[]
│    └── class?: string
│
├─── Data Processing
│    ├── Filter by type (if specified)
│    ├── Sort by date
│    ├── Apply limit
│    └── Group by month (optional)
│
├─── Render Logic
│    ├── Grouped View (with month headers)
│    │   └─── For each month:
│    │        ├── Month header
│    │        └── Update items
│    │
│    └── Flat View (no grouping)
│         └─── Update items
│
└─── Update Item Card
     ├── Header
     │   ├── Date + Type Badge
     │   └── Title (clickable if URL)
     │
     ├── Type-Specific Metadata
     │   ├── Publications: venue, authors
     │   ├── Talks: event, location
     │   └── Blog: tags
     │
     └── Description (truncated in compact mode)
```

## Data Flow

```
┌─────────────────┐
│  Content        │
│  Collections    │
│  (Astro)        │
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  updates.ts     │
│  (Utilities)    │
│                 │
│  • getAllUpdates()          ← Aggregates from all sources
│  • getRecentUpdates(n)      ← Limits to N items
│  • getUpdatesByType()       ← Filters by type
│  • groupUpdatesByMonth()    ← Groups data
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  Page/Layout    │
│  (Astro)        │
│                 │
│  const updates  │
│    = await      │
│    getAll...()  │
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  UpdatesList    │
│  Component      │
│                 │
│  <UpdatesList   │
│    items={...}  │
│    compact={..} │
│  />             │
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  Static HTML    │
│  Output         │
└─────────────────┘
```

## UpdateItem Type System

```
UpdateItem (Base Interface)
│
├── Required Fields
│   ├── type: 'blog' | 'publication' | 'talk' | 'teaching' | 'project'
│   ├── title: string
│   └── date: Date
│
├── Optional Common Fields
│   ├── description?: string
│   └── url?: string
│
├── Type-Specific Fields (Optional)
│   ├── Publications
│   │   ├── venue?: string
│   │   └── authors?: string[]
│   │
│   ├── Talks
│   │   ├── event?: string
│   │   └── location?: string
│   │
│   └── Blog
│       └── tags?: string[]
│
└── Visual Enhancement Fields
    ├── icon?: string         (e.g., "🏆")
    └── highlight?: boolean   (accent border)
```

## Type Badge System

```
┌──────────────────────────────────────────┐
│          Type Badge Configuration         │
├──────────┬──────────┬────────────────────┤
│   Type   │  Variant │  Visual Style      │
├──────────┼──────────┼────────────────────┤
│   blog   │ primary  │ Red accent bg      │
│   paper  │ success  │ Green bg           │
│   talk   │ warning  │ Amber bg           │
│ teaching │ outline  │ Border only        │
│ project  │ default  │ Gray bg            │
└──────────┴──────────┴────────────────────┘
```

## Display Mode Comparison

```
┌─────────────────────────────────────────────────────────────┐
│                    Compact Mode                              │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────┐           │
│  │  Date • Badge                                │ ← 3rem    │
│  │  Title                                       │   gap     │
│  │  Short description (max 120 chars)...       │           │
│  └──────────────────────────────────────────────┘           │
│  ┌──────────────────────────────────────────────┐           │
│  │  Date • Badge                                │           │
│  │  Title                                       │           │
│  └──────────────────────────────────────────────┘           │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      Full Mode                               │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────┐           │
│  │  Date • Badge                                │           │
│  │  Title                                       │           │
│  │  Venue: Conference 2024                      │ ← 4rem    │
│  │  Complete description with all the details   │   gap     │
│  │  that can span multiple lines...             │           │
│  │  [Tag1] [Tag2] [Tag3]                        │           │
│  └──────────────────────────────────────────────┘           │
│                                                              │
│  ┌──────────────────────────────────────────────┐           │
│  │  Date • Badge                                │           │
│  │  Title                                       │           │
│  │  Full content...                             │           │
│  └──────────────────────────────────────────────┘           │
└─────────────────────────────────────────────────────────────┘
```

## Month Grouping Structure

```
With groupByMonth={true}:

┌─────────────────────────────────────────┐
│  January 2024                           │ ← Month header
├─────────────────────────────────────────┤
│  ┌───────────────────────────────────┐  │
│  │  Jan 15 • Blog                    │  │
│  │  Title of Blog Post               │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  Jan 10 • Paper                   │  │
│  │  Publication Title                │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  December 2023                          │
├─────────────────────────────────────────┤
│  ┌───────────────────────────────────┐  │
│  │  Dec 20 • Talk                    │  │
│  │  Conference Presentation          │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

## Update Item Card Anatomy

```
┌─────────────────────────────────────────────────────────┐
│  ┌─────────────────────────────────────────────────┐   │
│  │  [Date: Jan 15, 2024] [Badge: Blog]            │   │ ← Header
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  [🏆] Title of the Update (clickable)          │   │ ← Title
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  Venue: ICML 2024 · San Francisco              │   │ ← Metadata
│  └─────────────────────────────────────────────────┘   │   (conditional)
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  Description text that explains what this       │   │ ← Description
│  │  update is about and provides context...        │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  [ML] [AI] [Research]                           │   │ ← Tags
│  └─────────────────────────────────────────────────┘   │   (optional)
└─────────────────────────────────────────────────────────┘
       ↑
  Highlight: Left accent border (if highlight={true})
```

## CSS Architecture

```
UpdatesList Component Styles
│
├── Container (.updates-list)
│   ├── Width: 100%
│   └── Compact modifier
│
├── Updates Group (.updates-group)
│   ├── Display: flex column
│   ├── Gap: var(--space-4) or var(--space-3)
│   └── Stagger animations
│
├── Month Section (.month-section)
│   ├── Month Label (.month-label)
│   │   ├── Font: large, medium weight
│   │   ├── Color: muted
│   │   └── Border bottom
│   └── Updates Group
│
└── Update Item (.update-item)
    ├── Padding: var(--space-4)
    ├── Border radius: medium
    ├── Hover: background color change
    ├── Highlight: left accent border
    │
    ├── Header (.update-header)
    │   ├── Meta (.update-meta)
    │   │   ├── Date (monospace font)
    │   │   └── Type Badge
    │   └── Title (.update-title)
    │       └── Optional link wrapper
    │
    ├── Metadata (conditional)
    │   ├── Venue (.update-venue)
    │   └── Authors (.update-authors)
    │
    ├── Description (.update-description)
    │   └── Truncated in compact mode
    │
    └── Tags (.update-tags)
        └── Badge components
```

## Integration Points

```
┌──────────────────────────────────────────────────────┐
│              Astro Content Collections               │
├──────────────────────────────────────────────────────┤
│  • blog/                                             │
│  • publications/                                     │
│  • talks/                                            │
│  • teaching/                                         │
│  • projects/                                         │
└─────────────────────┬────────────────────────────────┘
                      │
                      ↓
┌──────────────────────────────────────────────────────┐
│              src/lib/updates.ts                      │
├──────────────────────────────────────────────────────┤
│  • Aggregation functions                             │
│  • Filtering utilities                               │
│  • Grouping helpers                                  │
└─────────────────────┬────────────────────────────────┘
                      │
                      ↓
┌──────────────────────────────────────────────────────┐
│              Page Templates                          │
├──────────────────────────────────────────────────────┤
│  • Homepage (compact, limited)                       │
│  • Updates page (full, grouped)                      │
│  • Filtered views (research, blog, etc.)             │
└─────────────────────┬────────────────────────────────┘
                      │
                      ↓
┌──────────────────────────────────────────────────────┐
│              UpdatesList Component                   │
├──────────────────────────────────────────────────────┤
│  • Renders unified timeline                          │
│  • Handles display modes                             │
│  • Type-specific metadata                            │
└──────────────────────────────────────────────────────┘
```

## State Management

```
┌─────────────────────────────────────────┐
│         Build-Time (Static)             │
├─────────────────────────────────────────┤
│  • Fetch all content collections        │
│  • Aggregate into UpdateItem[]          │
│  • Sort by date                          │
│  • Apply filters                         │
│  • Generate static HTML                  │
└─────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────┐
│         Runtime (Optional)              │
├─────────────────────────────────────────┤
│  • Client-side filtering (via JS)       │
│  • Interactive type toggles              │
│  • Search functionality                  │
└─────────────────────────────────────────┘

Note: Component works without any runtime JS
      All functionality available at build-time
```

## Performance Profile

```
Build Phase:
  ├── Content Collection Queries: O(n) per collection
  ├── Aggregation: O(n) total items
  ├── Sorting: O(n log n)
  ├── Grouping: O(n)
  └── HTML Generation: O(n)

Runtime:
  ├── JS Size: 0 bytes (pure HTML/CSS)
  ├── CSS Size: ~3KB
  ├── HTML Size: Variable based on item count
  └── Interaction: Pure CSS hover (no JS)

Optimization:
  ✓ Zero runtime JavaScript
  ✓ Static HTML generation
  ✓ CSS-only interactions
  ✓ No client-side data fetching
  ✓ Build-time sorting/filtering
```

## Dependency Graph

```
UpdatesList.astro
├── Badge.astro (UI component)
├── formatDate() from src/lib/blog.ts
├── UpdateItem (type definition)
└── CSS Variables from design system

src/lib/updates.ts
├── getCollection() from astro:content
├── getPublishedPosts() from src/lib/blog.ts
└── UpdateItem (type definition)

No external dependencies
No npm packages required
Pure Astro + TypeScript
```

## Extension Points

```
UpdatesList Component
│
├── Custom Type Support
│   └── Add new types to UpdateItem union
│
├── Metadata Extensions
│   ├── Add new optional fields
│   └── Implement conditional rendering
│
├── Styling Customization
│   ├── CSS variables
│   ├── Class props
│   └── Theme overrides
│
├── Filtering Enhancement
│   ├── Multiple filter criteria
│   ├── Date ranges
│   └── Search integration
│
└── Display Modes
    ├── Custom layouts
    ├── Grid view
    └── Card variations
```

---

This architecture enables:
- **Flexibility**: Handles multiple content types
- **Performance**: Build-time static generation
- **Maintainability**: Clear separation of concerns
- **Extensibility**: Easy to add new features
- **Simplicity**: No complex state management
- **Accessibility**: Semantic HTML structure
