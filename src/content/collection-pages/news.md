---
title: "News & Updates"
description: "Recent updates including blog posts, talks, and publications."
collection: "news"

# View System Configuration
# Available views: "timeline" | "showcase" | "grid" | "minimal"
view: "timeline"

# Timeline View Options
viewConfig:
  # Style
  style: "minimal"             # "minimal" (clean rows) | "strip" (vertical line with connected dots)

  # Grouping
  groupBy: "year"              # "year" | "none" - Group items by year

  # Dot indicator
  showDot: true                # Show colored dot indicator (only for minimal style)
  dotType: "content"           # "content" (by type: blog=accent, talk=cyan, pub=green) | "status"

  # Date display
  showDate: true               # Show date column
  dateFormat: "short"          # "short" | "long" | "month-year" | "year"

  # Content display
  showDescription: false       # Show description text
  descriptionLimit: 150        # Max characters for description
  showContext: true            # Show context line (event, venue for talks)
  showAuthors: true            # Show authors (for publications)
  authorLimit: 3               # Max authors before "et al."
  showTags: false              # Show tags below title
  tagLimit: 3                  # Max tags to show

  # Links
  showLinks: ["pdf", "slides", "video", "code"]
  linkStyle: "text"            # "icons" | "text" | "both"

# Sorting
sortBy: "date"
sortOrder: "desc"
---

Stay updated with my latest blog posts, talks, and publications all in one place.
